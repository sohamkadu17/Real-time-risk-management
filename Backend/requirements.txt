# Core dependencies
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.0

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.18

# Redis (optional)
redis==5.2.0

# Streaming — Pathway reactive data processing framework
# ──────────────────────────────────────────────────────
# Pathway ships pre-built wheels for Linux x86-64 and ARM64.
# On Windows the app auto-falls back to a threading engine (see
# app/streaming/pathway_pipeline.py — PathwayPipeline.PATHWAY_AVAILABLE).
#
# Linux / Docker install:
#   pip install "pathway>=0.14.0,<1.0.0"
#
# The Dockerfile installs it automatically (see system-dep comments there).
pathway>=0.14.0,<1.0.0

# Scientific Computing (for Black-76 model)
numpy>=1.24.0
scipy>=1.11.0

# Utilities
python-json-logger==2.0.7
httpx==0.27.2
pytz==2024.1

# AI/LLM (Google Gemini - Free API)
google-generativeai>=0.8.0
email-validator

# Testing
pytest==8.0.0
pytest-asyncio==0.23.0
